---
title: "GIC Lab"
subtitle: "Open, controlled research on learned agent architecture"
---

::: {.callout-note}
## Phase 1 is active; T09 Retry 3 stopped before empirical entry

Phase 0.75 is complete. Phase 1 is open for sequential artifact-execution work, but
paid compute, prototype execution, benchmark execution, training, and cloud mutation
all remain disabled. The T07 one-step SiRA artifact smoke has been executed and
offline-adjudicated; both sessions were incomplete, no scientific measurement or
EXP-0001 outcome was assigned, and its execution authority is exhausted. The tracked
Historical `PLAN-EXP0001-PILOT-V3` remains unauthorized (`authorized: false`). Retry
2 qualified and froze one replacement runtime under `PLAN-EXP0001-PILOT-V4`, then ran
Task A reactive exactly once. The task completed and the pinned evaluator produced a
descriptive score of 0.0 from retained evidence. Two frozen host-finalizer control
defects required one network-disabled reconstruction of that already-consumed attempt;
the corrected invocation could not conformantly carry into later conditions. Task A
simulative and both Task B attempts did not run, so there is no pair, checkpoint,
condition comparison, or EXP-0001 outcome. The exact host was terminated, zero T09
instances and restored security were verified, and the single-use authority is
exhausted. Retry 3 repaired and offline-verified both finalizer primitives against the
retained V4 evidence, then consumed its two authorized pre-empirical launches without
starting a V5 attempt. The first stopped on a private archive path error; the second
stopped immediately after provider activation left less than one 3,600-second
condition wall plus the 900-second cleanup reserve. Both resources are terminal or
absent, zero T09 instances and restored security are verified, Retry3 model/browser/
attempt usage is zero, and its authority is exhausted. EXP-0001 remains unevaluated.
:::

GIC Lab studies a narrower and more falsifiable question than “can we build a GIC agent?”:

> Which proposed mechanisms are necessary, sufficient, learnable, and measurably better than matched external orchestration?

The program begins with the authors' released SiRA and SR²AM references, then isolates one mechanism at a time under declared resource budgets. Negative and inconclusive evidence is a valid outcome.

## Phase 1 boundary

- Execute work packages sequentially and only under their exact current-turn human
  authorization; analysis tasks do not inherit execution authority.
- Keep the first smoke to one matched SiRA reactive/simulative pair and prohibit
  scientific interpretation.
- Preserve raw evidence, version/hash identity, budgets, attempts, and verified cleanup.
- Record the fixed SiRA mode as `experiment_assignment`, never learned regulation.
- Treat any planned/deferred RQ-H2K trace-sufficiency output as infrastructure evidence
  only; it adds no experiment, condition, training path, learned kernel, or later phase.
- Keep reusable tracked pilot authority, SR²AM cloud runs, and training disabled;
  neither T09 V3, V4, nor V5 grants replay authority. A future paired calibration
  requires fresh identities, campaign clock, review, and new authorization.
- Treat the T07 smoke's usage and trajectories only as artifact-execution evidence;
  they are neither pilot observations nor representative full-task costs.

See [project status](generated/status.qmd), the [claim map](research/claims.qmd), and the generated [experiment registry](generated/experiments.qmd).
