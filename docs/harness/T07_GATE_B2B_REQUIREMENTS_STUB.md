# T07 Gate B2b requirements stub

Status: **requirements only; undesigned and unauthorized**

This document contains no Gate B2b plan identity, digest, executable argument array,
execution command, or authorization block. Gate B2b may be designed only after a
separately authorized Gate B2a produces and seals all observations below.

Required B2a outputs are:

- freshly verified official Docker artifact identity and provenance;
- exact installed app/build, client/server, server OS/architecture, security, cgroup,
  and context evidence;
- resolved numeric Mac mini operational floor and before/after observations;
- exact external volume UUID, physical-store UUID, APFS, writable/unlocked, UTDM,
  capacity/free, and path-guard evidence;
- version-specific persisted disk-location setting/key and a minimized hashed extract;
- cropped disk-location-only GUI evidence and bounded user attestations;
- exact external VM disk file/device/inode/logical/allocated identity and open-file
  evidence;
- verified absence/inactivity of the exact version-specific default internal VM disk;
- classified list of unavoidable first-party bootstrap resources and proof that no
  T07 workload resource exists;
- clean-stop, disconnect, reconnect, UUID/path requalification, same-disk healthy
  reopen, and final-stop evidence;
- sealed B2a evidence, verified one-way external copy, copy record, and retained local
  source;
- proof that no image pull/build, container/browser, dependency, API, secret, or SiRA
  action occurred.

Only then may a later task bind immutable runtime/image/storage identities, fresh
attempt identities, no-network synthetic containment and browser-only fixtures,
dummy-secret leakage checks, exact per-probe/aggregate caps, cleanup, and stop
conditions. Both SiRA conditions and Gate C remain outside that future probe gate.
