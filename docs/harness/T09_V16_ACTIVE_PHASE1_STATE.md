# T09 V16 active Phase 1 state

V15 entered only Task A reactive, which is consumed, incomplete, exported, and
immutable. The other three conditions did not run. No pair exists and no scientific
comparison is allowed. Provider and security cleanup is verified.

V16 repairs finite role-specific Git-blob validation and boundary-aware structural
credential heuristics. The exact selector is admitted under its 4 MiB role cap;
smaller role caps remain independent. The production offline preflight is clean,
true-positive canaries remain detected, and empirical-prefix cleanup publishes and
reuses terminal receipts byte-identically.

The active proposal has fresh V16/AUTONOMOUS-0009 host, attempt, evaluator,
qualification, archive, stage, and manifest identities. V15 evidence is ineligible for
V16 pairing. Science is unchanged. Category 3 is unauthorized, all V16 flags are
false, and no V16 overlay, provider resource, frozen manifest, run root, or attempt
exists.
