# T07 bounded-smoke V3 implementation ledger

Status: **complete; ready for a fresh bounded-smoke V3 authorization; unauthorized**

Starting commit: `cac0ff114973a293426c7278fc7a4f6089953c65`

Reviewed implementation ancestor: `a7ca7475177aee60126e39c631d61e3d9453ca85`

Plan: `PLAN-T07-BOUNDED-SIRA-SMOKE-V3`, 51,401 bytes, SHA-256
`30e83897c476dbd403a55d9d128636443f9df8a787ef903665ece083e3e41a53`.

## Assembly definition-of-done map

| ID | Contract obligation | Evidence | Status |
|---|---|---|---|
| V3-01 | Verify exact branch/clean starting commit, V2 plan identity, zero V2 roots/requests/mutations/checkpoints/cost/secret accesses, and locked science before editing. | Local Git/hash/root checks and prior blocked disposition; no external or secret operation. | met |
| V3-02 | Inspect only the exact previously approved `.env` metadata and assignment structure. | Regular no-follow current-user file, mode 0644, no group/world write, repository-external/ignored, exactly one OpenAI and one Lambda assignment, reviewed plain syntax; no value access. | met |
| V3-03 | Replace the permanent `SIRA_API_KEY`-file prerequisite with the existing `OPENAI_API_KEY` source. | Exact V3 secret-source contract/schema; `user_created_sira_api_key_file_required=false`; no fallback or full-env upload. | met |
| V3-04 | Implement strict no-shell parsing and shortest-lived value handling. | Held `O_NOFOLLOW` traversal; regular/owner/mode/link/size checks; closed syntax; duplicate/missing/empty rejection; mutable zeroed lease; deferred remote read until model preflight; no shell/subprocess or credential hash. | met |
| V3-05 | Keep Lambda and OpenAI channels disjoint. | Lambda observer takes one `LAMBDA_API_KEY` only after preflight; OpenAI loader selects only `OPENAI_API_KEY`; remote/bootstrap/child inherited-environment guards; cross-channel tests. | met |
| V3-06 | Supply pinned SiRA without user duplication. | Automatic filtered mode-0600 file; native `OPENAI_API_KEY` metadata child; immediate-condition-child ephemeral `SIRA_API_KEY` alias; no process receives both. | met |
| V3-07 | Prevent pre-upload path redirection or stale-target reuse. | Fixed parent-create array; ownership/mode checks; exact realpath result; absent/non-symlink target checks before materialization; regular/non-symlink/current-user/exact-0600 checks after upload. | met |
| V3-08 | Enforce success/failure cleanup and credential-rotation semantics. | Dev/inode-bound local cleanup; typed definite/unknown abort paths; emergency cleanup survives expiry/repository drift; remote receipt validation; unresolved cleanup/usage remains unresolved. | met |
| V3-09 | Exclude credential values/derivatives from every retained or public surface. | Runtime file excluded from Git/upload/evidence; no value/hash in receipts; direct/hex/base64 and semantic scans before hashing untrusted output; canary negative tests. | met |
| V3-10 | Preserve V1/V2 plans and burn their run identities. | Exact V1/V2 byte/hash regression; fresh V3 roots and `0003` IDs; no rewrite or replay. | met |
| V3-11 | Reuse the V2 private security binding only if secret-neutral and exact-sealed. | Schema/source inspection; exact decision seal, baseline seal, restoration payload, alias/hash/firewall/resource contents; tamper regressions. | met |
| V3-12 | Preserve all scientific fields and every V2 budget/cap. | Five locked hashes plus exact V2/V3 `scientific_lock` and `limits` equality tests. | met |
| V3-13 | Generate one executable-but-unauthorized V3 plan and fresh packet/runbook. | V3 plan/schema/packet and exact 29-step runbook; current permissions all zero/false. | met |
| V3-14 | Run focused loader/channel/bootstrap/plan/supervisor/budget/evidence/cleanup tests. | 299 focused container/secret/smoke/supervisor/schema/closeout tests passed. | met |
| V3-15 | Run schema/repository validation, privacy scans, Ruff, and strict mypy. | Repository and 34-artifact validation, tracked/unignored privacy scans, Ruff, strict mypy over 57 source files, and `git diff --check` passed. | met |
| V3-16 | Run full accepted portable-Quarto `make check`. | Post-review `make check` passed 1,247 tests, repository and site validation, and a 16-page Quarto 1.9.38 render; only the known nonfatal output-path warning remained. | met |
| V3-17 | Obtain independent scientific-scope/privacy/credential/spending/cleanup review, repair, and rereview. | Independent post-repair rereview returned CLEAN with no P0/P1/P2 findings; historical immutability, science, privacy, credential, spending, cleanup, packet, and runbook contracts passed. | met |
| V3-18 | Commit on the required branch, leave a clean tree, and stop before authorization/execution. | Final handoff supplies the containing commit SHA and verifies the clean branch; the plan remains unauthorized with every execution permission false. | met |

## Preserved limits and archive arithmetic

V3 retains the complete V2 limit object unchanged: USD 4.00 aggregate/USD 2.00 per
condition OpenAI cost; 400,000/200,000 tokens; 77 calls total (16/61); one attempt,
one browser action, 120 seconds, and 104,857,600 output bytes per condition; zero
retry; one Lambda instance/launch click, zero persistent filesystem, USD 2.00 and
3,600 seconds with termination by 3,300; 13 Lambda GETs; and every V2 container,
process, transfer, disk, evidence, storage, archive, and cleanup cap.

The tracked upload remains exactly 36 members: one manifest, the V3 plan, and 34
tracked implementation artifacts. The new local-only secret-source schema is bound
separately by exact SHA-256 and is not a remote runtime input. A complete external
archive is now explicitly tested at 39 payload plus three seal files: nine base local
records, four local-secret lifecycle records, thirteen responses, five phase reports,
three inbound files, three release records, and two reviewed upload artifacts. The
unchanged cap remains 125 payload plus three seals.

## Execution boundary

No real secret or external account endpoint was accessed. No Lambda/OpenAI/public-IP
request, cloud mutation, billable work, SSH, Jupyter, container, browser, SiRA
condition, or scientific execution occurred. The V3 plan remains unauthorized and
all execution permissions remain false.
