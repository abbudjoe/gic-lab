# T09 V16 preauthorization packet

Status: **Category 1 implementation complete; offline validation and exact-head review required; unauthorized**.

This packet is not authorization. V16 permits no real secret access, authenticated
model request, provider request, cloud mutation, live Docker/browser, SiRA/evaluator,
authorization overlay, frozen manifest, run root, or scientific attempt. Offline
tests, a draft PR, independent exact-head ChatGPT review, and a separate reviewed merge
are prerequisites to any later fresh Category 3 authorization.

V15 reached empirical entry only for Task A reactive. That identity is consumed, the
task remained incomplete, 24 calls and 49,642 tokens were reconciled, and immutable
raw evidence was exported. Provider termination, zero T09 instances, firewall/ruleset
restoration, no credential exposure, and no core artifact were verified. No pair was
realized, the attempt is not reusable in V16, and no scientific comparison is allowed.

The V15 finalizer stopped before evaluator completion because the exact committed
1,084,958-byte selector exceeded an unrelated one-MiB ceiling. V16 uses exact
role/path Git blobs, finite role caps, no-follow metadata, bounded byte equality, and
content-free receipts. The selector cap is 4,194,304 bytes; finalizer, projection, and
schema caps remain 1,048,576 bytes.

The public cleanup certificate was separately blocked because an `sk-` heuristic
lacked a left lexical boundary and matched a suffix of `task-b-normalization-edge`.
V16 applies explicit ASCII token boundaries to OpenAI, AWS, and Bearer detectors. The
production-generated offline preflight is clean, while runtime-built true-positive
canaries and exact-secret scanning remain fail closed. Empirical-prefix cleanup
publishes and resumes byte-identically without repeating mutations.

Fresh V16 identities use `PLAN-EXP0001-PILOT-V16`, host
`RUN-T09-PILOT-HOST-AUTONOMOUS-0009`, four `AUTONOMOUS-0009` attempts/evaluators,
V16 qualifications, and `RUN-MANIFEST-EXP0001-PILOT-V16-AUTONOMOUS-0009`. Every
authorization/execution flag remains false.

Prior cumulative T09 cost is exactly USD `36.36170860803283125`; remaining cumulative
headroom is USD `53.63829139196716875`. Component ceilings remain USD 10 preflight
Lambda, USD 8 empirical Lambda, and USD 40 OpenAI. The scientific contract is unchanged.
