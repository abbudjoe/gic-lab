# Decision Log

Only explicit, reviewable decisions belong here. Historical estimates and source claims belong elsewhere.

| ID | Date | Decision | Rationale | Revisit trigger |
|---|---|---|---|---|
| D-001 | 2026-07-15 | Use one canonical monorepo for code, protocols, public notebook source, and small results. | Keeps each research statement tied to code and configuration history. | Governance or release cadence materially diverges. |
| D-002 | 2026-07-15 | Generate a static Quarto site from `notebook/` and publish it through GitHub Pages. | Provides a navigable public surface without a second content source or dynamic backend. | Quarto cannot represent a validated publication requirement. |
| D-003 | 2026-07-15 | Store large artifacts externally and commit identity, hashes, provenance, and summaries. | Git is the provenance/control layer, not the binary artifact store. | None; storage backend may change, identity contract may not. |
| D-004 | 2026-07-15 | Keep experiment lifecycle, evidence strength, and outcome in separate typed fields. | Prevents completed work from being mislabeled confirmed and infrastructure failure from becoming a scientific negative. | Only through a schema migration with compatibility tests. |
| D-005 | 2026-07-15 | Maintain separate instruction/control and scientific-evidence authority planes. | Safety policy cannot be overridden by a paper, and project prose cannot rewrite a primary source. | Only through explicit governance revision. |
| D-006 | 2026-07-15 | Phase 0 performs no prototype, benchmark, training, cloud, or paid-compute operation. | Builds the research control plane before irreversible or costly work. | Phase 0 completion plus explicit approval of a later phase. |
| D-007 | 2026-07-15 | Defer repository license selection. | The user has not chosen code/content licensing. | Resolve OQ-001 before public release. |
| D-008 | 2026-08-06 | Use separate canonical claims for architectural mechanisms, conditional theorems, and empirical applicability. | A valid theorem under assumptions is not evidence that an implemented GIC component satisfies those assumptions. | A later source revision materially changes theorem scope. |
| D-009 | 2026-08-06 | Treat immutable goal authority, permissions, safety obligations, evaluator roots, and promotion authority as GIC Lab governance proposals, not as features established by the paper. | The paper allows identity to include evolving values and does not specify these immutable control-plane boundaries. | A reviewed project safety architecture replaces these provisional boundaries. |
| D-010 | 2026-08-06 | State Theorem 4's perfect-world-model result as non-inferiority, not guaranteed strict improvement. | The displayed inequality proves `V_mix >= V_env`; the surrounding “outperforming” language is stronger than the formal result. | A corrected source revision or stronger proof establishes strict improvement conditions. |
| D-011 | 2026-08-06 | Give environment synchronization one owner: the canonical sync target installs the local package non-editably, and all validation commands run with `uv run --no-sync`. | Plain `uv run` silently reversed the non-editable install, recreated a hidden editable `.pth`, and broke later imports. Separating environment construction from execution removes contradictory control paths. | `uv` or Python provides a verified editable-install contract immune to the hidden-file behavior. |
| D-012 | 2026-08-06 | Publish the canonical repository at `abbudjoe/gic-lab` and the notebook through GitHub Pages using GitHub Actions. | Makes the research record publicly navigable while preserving the monorepo as its canonical source. | Repository ownership or hosting requirements change. |
| D-013 | 2026-08-06 | Publish without selecting a software or content license; grant no reuse permission until OQ-001 is resolved. | Public visibility and reuse rights are distinct. This preserves the user's explicit decision to defer license selection without implying an open-source license. | OQ-001 is resolved and license files are committed. |
| D-014 | 2026-08-08 | Preserve RQ-H2K as a planned/deferred evidence track by adding a source-neutral regulation-decision contract and source-grounded SiRA/SR²AM trace requirements, without changing or registering EXP-0001. | A narrow T04.5 retrofit keeps a future comparison possible while preventing fixed experiment assignment or ordinary reasoning prose from being mislabeled as learned internal regulation. | A separately approved package supplies an operational definition, matched protocol, safety/fallback controls, and explicit implementation/training authority. |
| D-015 | 2026-08-08 | Close Phase 0.75 and open Phase 1 with every execution and compute permission false; make only `PLAN-EXP0001-SMOKE` eligible for a later exact authorization. | Separating authorization eligibility from required T07 preflight keeps a proposed plan and budget from becoming execution authority while leaving one unambiguous next action. | A current-turn authorization names the exact plan, provider/model, cost, time, and cleanup contract, or Phase 1 scope changes. |
| D-016 | 2026-08-08 | Materialize the first SiRA smoke from a clean external checkout at the pinned source commit rather than vendoring or adding a submodule in T07. | This preserves immutable upstream identity without changing repository structure or mixing runtime artifacts into Git. | A reviewed source patch, offline-reproducibility requirement, or upstream availability failure requires a different source-distribution strategy. |
| D-017 | 2026-08-09 | Use `/Volumes/Macintosh HD - Data/Users/joseph/.local/share/gic-lab` on MacBook Pro storage exported to the Mac mini through Thunderbolt Target Disk Mode, APFS volume UUID `8478609D-FA37-4ED5-875D-47AE912B9151`, as the preferred destination for sealed, immutable artifact retention; active attempt, build, and Docker VM/image storage require separate positive suitability evidence and have no internal-disk fallback. | The user approved the MacBook Pro storage for durable artifacts while explicitly withholding any assumption that capacity alone clears Docker runtime storage. Topology-aware identity and sealed-copy verification prevent Target Disk Mode loss, missing mounts, or mutable sync from silently corrupting provenance. | The user selects a reviewed execution/storage topology, the volume is re-exposed through a different mechanism, or the drive is migrated through an identity-and-hash-preserving storage plan. |
| D-018 | 2026-08-09 | Supersede the unused D-017 path for new T07 writes with dedicated root `/Volumes/Macintosh HD - Data/GIC-Lab`: sealed artifacts use `t07/sealed-artifacts`, while `t07/docker-desktop/disk-image` and `t07/docker-build-staging` remain qualification candidates only; live attempts remain on the Mac mini and no internal fallback is allowed. | A new top-level project root avoids MacBook user/system/unrelated directories and keeps Docker, staging, sealed archives, and active attempts as non-interchangeable roles. Capacity and control-plane tests do not substitute for the later authorized Docker placement/reconnect evidence. | A separately authorized Gate B2a resolves every numeric system-floor and version-specific Docker blocker and passes placement/reconnect qualification, or the user selects another topology. |
| D-019 | 2026-08-09 | Terminate the current T07 local-runtime selection: retain D-018's sealed-artifact root, treat its Docker Desktop qualification path as superseded blocked provenance, and reject the reviewed Colima v0.10.3/Lima v2.2.0 topology without issuing an executable B2a plan. | Required external `LIMA_HOME` would place Lima's private SSH identity on the `noowners` UTDM volume; the supported VM image also does not provide a current pre-start immutable Docker runtime, and numeric first-start/storage terms remain unresolved. Inventing a source fact, cap, or ownership boundary would violate the binary Gate B1.7 decision rule. | The user explicitly selects and separately reviews one of: a directly attached local SSD on the Mac mini, a Linux execution host, or a deliberately weaker containment/storage-confidentiality governance contract. No new B1.x design gate follows automatically. |
| D-020 | 2026-08-09 | Select a short-lived x86_64 Lambda On-Demand Cloud host as T07's planned design-only substrate, with no persistent Lambda filesystem; preserve the Mac-local Docker/Colima decisions as terminal rejected provenance; require separate L1 GET-only inventory, L2 host qualification, L3 workload qualification, and L4 live-pair authorizations. | The user selected D-019's separately approved Linux-host alternative. Lambda's typed public API can source-bind architecture, price, capacity, image, key, firewall and instance identity, while provider-API termination supplies an authoritative ephemeral billing boundary. L1 evidence must be hash-verified in the approved external archive; L2 also requires authenticated SSH server-host-key trust. Separating the four gates prevents account inspection from implying mutation, host qualification from implying browser/model execution, or substrate choice from changing EXP-0001. | L1 finds no <=USD 1.50/hour qualifying x86_64 capacity or no SSH-only existing ruleset; authenticated SSH host-key bootstrap remains unavailable; L2 fails runtime/containment/termination qualification; the user changes provider/storage/governance; or a reviewed Gate L4 completes the one locked pair. |
| D-021 | 2026-08-09 | Preserve both blocked Gate L1 V1 attempts as historical evidence, permanently retire `RUN-T07-L1-LAMBDA-INVENTORY-0001`, and require the fresh V2/0002 inventory path to durably commit an allowlisted, append-only request ledger before every possible account request. | The latest V1 attempt exited through a generic secret-safe failure path without evidence sufficient to determine whether a request was sent. A durable intent/send/terminal ledger distinguishes locally knowable states without claiming network exactly-once semantics or blaming the provider, key, endpoint, network, or transport. | A separately authorized V2 inventory succeeds with a complete validated and sealed ledger, or an independently reviewed contract revision creates another fresh plan/run identity. |
| D-022 | 2026-08-10 | Preserve stopped run `RUN-T07-L1-LAMBDA-INVENTORY-0002` as nonreplayable historical evidence, classify its exact schema mismatch as `unadjudicated_raw_body_absent`, and replace its future path with an unauthorized seven-GET V3 inventory that omits audit history and account-LRN retrieval. | The durable ledger proves one HTTP-200 audit response and a parser-declared schema drift, but the raw body was not retained, so the exact live structure cannot be reconstructed. Audit history is not required for Gate L2 and broadens privacy/pagination exposure. Endpoint-specific schemas can enforce documented required fields/types, accept only compatible additions, retain no unknown values, and stop on any continuation without inventing account identity. | A separately authorized V3/0003 inventory succeeds with a complete validated and externally sealed ledger, the official launch contract proves an account LRN is required, or a future official schema change requires another reviewed immutable plan/run identity. |
| D-023 | 2026-08-10 | Preserve successful sealed run `RUN-T07-L1-LAMBDA-INVENTORY-0003`; repair image identity as one opaque inventory-local alias per official `Image.id` with regional availability; and stop Gate L2 at `inventory-evidence-insufficient` without an executable plan. | Offline adjudication proves that 259 rows represent 124 nonconflicting image identities, so the duplicate finding was a verifier-key defect. Six resource tuples satisfy the fixed policy, but the privacy-minimized inventory retained account SSH-key names without public-key material, making the required local/account fingerprint equivalence unknowable. The global firewall also contains three non-SSH rules, and no regional ruleset exists; additive regional rules cannot replace global strictness. | Independently verified account public-key fingerprints or a new reviewed and separately authorized minimal read-only evidence plan supplies matchable key evidence; the user then chooses an exact candidate/key, global-firewall option and public `/32`, host-key trust method, and either a separately authorized strict same-region ruleset lifecycle or explicit reviewed supersession of D-020's regional-ruleset requirement. |
| D-024 | 2026-08-10 | Create a fresh unauthorized Gate L1A design for exactly one `GET /api/v1/ssh-keys`, with a new fsync-backed request ledger, in-process multi-format public-key fingerprints, held-no-follow local `.pub` matching, private/public evidence separation, and an authoritative post-ledger archive-finalization disposition; do not repeat broad inventory or select a key. | The official SSH-key response contains the missing public material, while one endpoint is the minimum account disclosure needed to compare the three already-known names. Exact OpenSSH wire fingerprints avoid text-hash ambiguity, and an ignored sealed raw/private bundle prevents public-key bodies, raw IDs, fingerprints, or home paths from entering Git. The request ledger can attest only staging because it must itself enter the archive, so a separate append-only fsync-backed disposition is required after atomic publication and post-copy verification. | A fresh current-turn authorization binds the exact clean execution commit and immutable plan; the one request yields a complete sealed `unique_match`, `no_match`, `ambiguous_match`, or `invalid_evidence` disposition with a passed finalization record; or a source/schema/privacy change requires another reviewed plan. Gate L2 remains separate and unauthorized. |
| D-025 | 2026-08-10 | Accept the sealed Gate L1A `fractal-lambda-codex` unique match and the user's private `gpu_1x_a10` / `us-east-1` / `img-0111` / no-filesystem / temporary strict-firewall / preloaded-agent / Jupyter-ED25519 choices, but stop Gate L2.0 at `blocked-human-or-source-decision` without an executable plan or authorization block. | Nonce-protected private parameters safely bind the human choices, but the first-party instance schema makes ownership name/tags optional and provides no strong-consistency, idempotency, or bounded-quiescence guarantee after an unknown launch response. Exact-ID termination can therefore not be guaranteed. Independent review also found no single supervisor enforcing response semantics, operation cardinality, aggregate limits, SSH lifecycle, evidence eligibility, and descriptor-held archive finalization. | A source-backed safe unknown-launch recovery contract and a newly reviewed end-to-end supervisor/evidence implementation create fresh, non-reused plan/run identities. |
| D-026 | 2026-08-10 | Remove local SSH fingerprints and local key paths from the current public run-0003 adjudication, while preserving its original Git object and leaving ignored/external sealed evidence unchanged. | The public adjudication needs only the evidence-unavailable conclusion; retaining local credential metadata was unnecessary disclosure. The prior file remains traceable at the required Gate L2.0 starting commit, while the current privacy-minimized file is bound to a new hash. | A reviewed public-evidence policy explicitly requires those values and supplies a safer publication contract. |
| D-027 | 2026-08-10 | Terminate the automated T07 Lambda API-launch path at `manual-console-launch-required`; retain Gate L2.1 ownership/journal/watchdog/cap code only as non-authoritative offline evidence, create no executable V2 plan or authorization block, and reject every V1/V2 Gate L2 plan/run identity. | First-party Lambda sources document marker fields and exact-ID termination but no launch idempotency. Independent review found that the fake-tested pieces do not compose one phase-exact effecting supervisor or independently surviving cleanup watchdog: mutation bodies/order, cleanup-on-every-failure, cross-process budgets/lease, duplicate revalidation, terminal polling, process arrays, and evidence closure remain incomplete. More assertion-level tests would not close that architecture. | Only a user-requested, newly reviewed human-operated console launch/observation/cleanup contract with fresh identities and fresh current-turn authorization; do not open another automated L2.1 design loop. |
| D-028 | 2026-08-10 | Preserve Gate L2.1's automated-path rejection and stop the proposed human-console/Jupyter topology at `blocked-human-image-selection`, with `img-0032` / Lambda Stack 22.04 / `22.4.5-2141` recommended but unselected and no executable plan or authorization block. | First-party Lambda documentation guarantees Docker and JupyterLab for Lambda Stack 22.04 but lists Docker without JupyterLab for the currently selected GPU Base 22.04 image. The sealed inventory contains four x86-64 Lambda Stack regional candidates. No pinned source proves a static `gpu_1x_a10` image matrix, so a future run must also fail closed before Launch unless the approved alias is offered after exact type/region selection; changing the private selection still requires new approval. | The user privately approves or rejects the recommended image and all manual-console decisions; an approval still requires fresh plan/run identities, current price/capacity/firewall/zero-instance revalidation, launch-wizard offeredness, independent review, and exact current-turn authorization before any mutation. |
| D-029 | 2026-08-11 | Accept the separately supplied private Gate L2M image/manual-action decision, expose only its aliases and cryptographic bindings, and render one executable-but-unauthorized manual-console qualification plan with a GET-only observer, 23 fixed actor steps, 13 single-use user checkpoints, exact caps and expiry. | The decision validates the Jupyter-capable Lambda Stack selection and every manual action/risk attestation without publishing its IPv4, nonce, raw resource IDs, fingerprints or paths. Keeping console mutations human-only avoids reviving the rejected unknown-outcome API-launch path; observer-only instance/terminal receipts prevent user attestations from substituting for provider evidence. Cleanup authority remains limited to the exact bound or owned-ruleset-attached IDs, while an unattached row stops for a new decision rather than silently broadening authority. | A fresh current-turn authorization binds the final clean commit, exact plan/hash, credential channel, manual actions, USD 2.00/3,600-second caps and cleanup; the plan expires before credential access at `2026-08-12T05:15:19.646016Z`; or a source/resource/storage/control-plane change requires a new immutable plan and review. |
| D-030 | 2026-08-11 | Preserve Gate L2M V3/run 0003 as a burned pre-mutation failure, reject its transformed global-firewall projection as exact restoration authority, and replace the manual authorization path with one fresh unauthorized read-only baseline-capture GET. | Observation 0006 projected all currently expected rule fields, but the observer retained neither the raw 660-byte provider response nor unknown raw-key structure. Because the official rule schema does not explicitly forbid additive properties, exact losslessness cannot be established and missing values cannot be inferred. A versioned description-aware multiset canonicalizer now makes old V3 fail its implementation binding. | A separately authorized one-GET capture completely retains, validates, seals and externally verifies the raw response plus exact restoration payload; only then may an offline fresh manual plan be reviewed. No capture result itself authorizes firewall mutation, paid compute, or Gate L2M/L3/L4. |
| D-031 | 2026-08-11 | Preserve the one-GET capture's original `schema_drift`, adjudicate its sole additive data-level metadata as `compatible_additive_top_level_extension`, seal the exact description-aware baseline/restoration contract, and freeze rather than delete the T07 high-assurance infrastructure track. | The complete retained response proves that every documented rule field is valid and that the only mismatch was compatible response metadata not forbidden by the pinned schema. The track produced reusable safety/evidence controls but also terminally rejected macOS containment, Docker Desktop, Colima/Lima, and automated Lambda launch; finishing production-grade orchestration would no longer be proportionate to the one-pair research smoke. | A separately governed `phase-1/sira-smoke-bounded` child defines a new topology, plan, identities, numeric caps, cleanup/evidence contract, review, and current-turn authorization under the Occam admission rule. The frozen Gate L2M/L3/L4 path and burned identities are never reused. |
| D-032 | 2026-08-11 | Adopt unauthorized bounded plan `PLAN-T07-BOUNDED-SIRA-SMOKE-V1` on `phase-1/sira-smoke-bounded` as T07's only prospective execution path, using one manually supervised Lambda/Jupyter host and one ordered reactive/simulative pair under the Occam admission rule. | The plan retains controls that protect scientific validity, credentials/privacy, spend, provider termination, and reconstructable evidence while recording seven explicit residual limitations. It binds a separately materialized single-run authorization and private resource record, a durable 13-GET observer ledger, prelaunch firewall verification, immutable source/model/runtime commands, numeric caps, file-only secret injection, tmpfs-only condition output with immutable-ID copy-out, failure evidence, exact user actions, provider terminal verification, and held-descriptor archival without reusing frozen Gate L2M/L3/L4 authority. | A fresh exact current-turn authorization and every dynamic preflight pass; the first matched pair or terminal infrastructure failure expires the profile. Any retry, topology/resource/model/price/storage drift, or unresolved material risk requires a new plan and identities. |
| D-033 | 2026-08-11 | Preserve bounded V1 and run `0001` as burned pre-request provenance and supersede D-032's prospective identity with unauthorized `PLAN-T07-BOUNDED-SIRA-SMOKE-V2` and host/condition run `0002` identities. | Offline adjudication classified the stale ruleset identity as `stale_high_assurance_name`, the baseline mapping as `materializer_baseline_bug`, and the combined cause as `both`. V2 uses a fresh private locator and binding, separately typed semantic-baseline/restoration identities, verified local-plus-external sealing, and exact reviewed-commit ancestry while leaving every scientific field, budget, spend cap, condition order, evidence requirement, and cleanup boundary unchanged. | V2 remains unauthorized until a fresh current-turn user authorization binds the final clean packet commit and every dynamic preflight passes. Any binding, plan, source, resource, price, storage, or implementation drift requires fresh identities and review; V1 is never replayed. |
| D-034 | 2026-08-12 | Preserve bounded V2 and all `0002` run identities as blocked pre-root/pre-request evidence and supersede its obsolete second-file secret prerequisite with unauthorized `PLAN-T07-BOUNDED-SIRA-SMOKE-V3` and fresh `0003` identities. | The existing repository-external `.env` already separates `LAMBDA_API_KEY` from the OpenAI provider credential under `OPENAI_API_KEY`. V3 adds a strict no-follow, non-shell, single-assignment parser; keeps the Lambda and OpenAI channels disjoint; automatically creates and destroys one unarchived mode-0600 upload file; and maps its bytes to an ephemeral `SIRA_API_KEY` alias only for the pinned SiRA child. No second permanent user-managed key file, full `.env` upload, secret hash, scientific-field change, budget change, or execution is introduced. | V3 remains unauthorized until a fresh current-turn authorization binds the final clean packet commit, exact plan/schema hashes, private source substitution, and every dynamic preflight. Any secret-channel, plan, resource, price, storage, or implementation drift requires fresh identities and review; V1/V2 are never replayed. |
| D-035 | 2026-08-12 | Accept T08's offline classification of the T07 Retry 2 smoke as `matched_pair_valid_with_documented_evidence_gaps`, keep both task completions unobserved and EXP-0001 scientifically unevaluated, and prepare the two-task `PLAN-EXP0001-PILOT` package without authorizing it. | All retained manifests and source/destination copies rehash; exact command/config diffs contain only approved treatment/identity/output differences; aggregate usage and USD 0.0403325 cost independently reconcile; and cleanup is verified with a disclosed nonmaterial private-retention gap for inactive ephemeral Jupyter fields. Two counterbalanced tasks are the minimum useful scoring/variance-plumbing pilot, but evaluator/runtime/protocol/provider/privacy prerequisites and a new exact user authorization remain open. | Prerequisites close under review and a fresh current-turn authorization binds the exact clean pilot package and caps, or new evidence invalidates pair/evidence/cleanup classification. |
| D-036 | 2026-08-13 | Preserve effect-oriented proposal `PLAN-EXP0001-PILOT` and lock calibration-only successor `PLAN-EXP0001-PILOT-V2`, with the same two frozen rows and counterbalance, exact unchanged upstream evaluator, pair-equal enforced limits, four fresh identities, an automatic Task-A checkpoint, private retention, and no execution authority. | Calibration cannot support an effect/variance/superiority claim. The exact evaluator deterministically exposes a Task A normalization sensitivity and a 0.5 score for the ordinary fully correct Task B fixture (not a proven ceiling), while the starting child model-call caps were not pair-equal or fully enforced. A fresh identity makes those material meaning/control changes explicit; static tests close scoring, reconstruction, spending, cleanup, and credential risks without live execution. | A fresh current-turn authorization binds the final clean V2 packet and its dynamic preflight passes, or a task/evaluator/runtime/model/budget/evidence change requires a new plan and review. Public raw release remains independently blocked. |
| D-037 | 2026-08-13 | Stop `PLAN-EXP0001-PILOT-V2` at `t09-pilot-blocked-material-risk`, keep every authorization false, and issue no executable authorization block. | The inherited GET-only T07 observer has a 3,600-second hard provider wall and 1,800-second normal termination target. Four plausible full-task attempts plus bounded staging, three possible 300-second cleanup checkpoints, terminal polling, sealing, and pretermination transfer cannot fit with a defensible cleanup reserve. The T08 14,400-second candidate ceiling has no source-compatible enforcement path, and building automated launch/termination or a watchdog would reopen the expressly prohibited T07 infrastructure design. | A future successor must close the exact provider wall/cleanup/transfer contract without weakening spend or billable-resource safety, then receive fresh identities, bindings, tests, independent review, and authorization. EXP-0001 remains planned, unevaluated, and pending; public raw release remains separately blocked. |
| D-038 | 2026-08-13 | Preserve blocked V2 and create pragmatic `PLAN-EXP0001-PILOT-V3` with a plan-driven 14,400-second actual-time campaign, 900-second cleanup reserve, 13,500-second termination cutoff, and fresh identities under the user's current-turn one-launch authorization. | The new source contract explicitly supersedes only V2's 3,600-second infrastructure wall and diagnoses simultaneous reservation of every theoretical maximum as the admission bug. V3 charges setup, each full 3,600-second condition wall, a 600-second evaluator/evidence handoff, a 60-second termination-dispatch margin, and cleanup to one billable clock; it admits only the next attempt's finite envelope rather than all future maxima. It changes no task, evaluator, model, SiRA revision, order, treatment, scoring, evidence, budget, retry, or interpretation field and creates no persistent cloud platform or watchdog. | Complete focused gates, independent review, clean pre-run freeze, exact dynamic preflight, and private single-use authorization/cloud ledger before one launch. After empirical entry, never change code/science or retry a condition; require every prior export to be verified and acknowledged before later entry, prioritize exact provider termination, and preserve calibration-only interpretation. |
| D-039 | 2026-08-13 | Stop the one authorized `PLAN-EXP0001-PILOT-V3` host at `t09-pilot-blocked-material-risk` before empirical entry, exhaust its launch authority, and preserve every scientific lock and all four unused condition identities. | Clean package `9dc7363561ec96812072e2c7824141d75b028332` launched exactly one `gpu_1x_a10` host with no persistent filesystem. Dynamic preflight rebuilt container `sha256:07875dc67336b90021df5ab920860a56268bbc9f3aada70accec23848d9905cf`, not required frozen identity `sha256:035edf61718e84a8156f4f0f7817b134b0ce31488d3f0b50bbfba2b4a30cc61c`; the frozen image was not retained as a verified loadable artifact. Substitution was prohibited. No model request, task browser action, condition, evaluator, or score occurred. The exact host is terminal/absent, zero T09 instances and restored security state are verified, and USD 0.414064252316667 Lambda / USD 0 OpenAI is reconciled. | A verified loadable copy of the exact frozen image becomes available, or a fresh successor requalifies and binds a replacement runtime without changing the scientific contract; either route requires fresh identities where applicable, clean freeze, independent review, dynamic preflight, and new current-turn authorization. Public raw release remains independently blocked. |
| D-040 | 2026-08-13 | Preserve V3 and supersede only its cross-run image-digest equality rule with current-turn-authorized `PLAN-EXP0001-PILOT-V4`, which builds one replacement from pinned inputs, accepts it only after exact functional qualification, and freezes its source-derived image ID before empirical entry. | The historical image ID is a bit-level built-artifact identity, not a scientific treatment. Retained evidence can verify the base, Containerfile, SiRA tree, Python/package/browser/evaluator/entrypoint/config contracts, but lacks the old build context and history needed to determine the precise digest cause. V4 keeps every task, model, evaluator, order, cap, zero-retry, evidence, and calibration boundary unchanged while correcting three stale runtime hashes and enforcing one metadata GET, zero task requests/actions, cumulative T09 spending, and one immutable image across all attempts. | Focused gates and independent review pass on a clean package; the one replacement qualifies and the immutable run manifest is written before any task call/action; otherwise cleanup and exact provider termination take priority and no scientific conclusion follows. |
| D-041 | 2026-08-13 | Stop `PLAN-EXP0001-PILOT-V4` after its one valid scored Task A reactive attempt, forbid later conditions under a changed finalizer invocation, exhaust the single-use authority, and assign no paired or scientific outcome. | Replacement image `sha256:e2603f5a…` passed functional qualification and was frozen before entry. Task A reactive ran once; retained evidence reconstructs as completed, exact-evaluator valid, score 0.0. The frozen host wrapper then read `output_root` from the wrong command-manifest level and selected an evaluator-only interpreter without the qualified control dependency closure. A one-time network-disabled reconstruction preserved the already-consumed attempt with zero new model/browser activity, but independent review found that applying this corrected invocation to later attempts would violate the post-entry code/evaluator/configuration freeze. Task A simulative and both Task B attempts therefore remained `not-run`; no pair/checkpoint or comparative inference exists. Evidence transfer, secret/container cleanup, one exact termination request, terminal/absent, zero-instance, security restoration, and USD 2.5308164556905757 cumulative T09 accounting are verified. | Repair and directly test both finalizer primitives, mint fresh plan/run/evaluator/pair/stage/archive/provider identities in an independently reviewed clean successor, and obtain new current-turn authority before another complete paired calibration campaign. Public raw release remains separately blocked. |
| D-042 | 2026-08-14 | Stop `PLAN-EXP0001-PILOT-V5` at `t09-pilot-blocked-material-risk` before empirical entry after consuming both authorized pre-empirical launches; preserve the repaired finalizer and retained image as infrastructure evidence, exhaust the nonreset campaign authority, and assign no new measurement or scientific outcome. | V5 twice reproduced the repaired pure finalizer against the immutable V4 archive with zero new model/browser activity. Launch 1 built and exported candidate image `sha256:abe8ed38…` but failed before metadata on a private archive path error. After exact terminal/zero/security closeout and source-bound eligibility, launch 2 passed the 4,500-second pre-POST headroom gate; provider activation then consumed the remaining margin, so it closed before host access or dynamic preflight. Across both launches, metadata/model/browser/evaluator/condition activity remained zero; two exact termination requests proved both resources terminal or absent, zero T09 instances, and restored security. Retry3 Lambda cost is USD 1.5106036795496942 and cumulative T09 cost is USD 4.04142013524027. | Preserve the sealed two-launch and image archives. Any future paired calibration must use fresh identities, a new campaign clock with explicit activation/preflight slack, independent review, and new current-turn authorization; no V5 identity or authority may be replayed. Public raw release remains separately blocked. |
| D-043 | 2026-08-14 | Preserve the exact frozen smoke/V5 profile and V5 execution-contract bytes as historical package evidence, and make `T09_PRAGMATIC_RETRY3_TERMINAL_CONTROL.json` the authoritative current execution-control overlay for their consumed identities. | Rewriting the frozen `4a4ecc1e…` package would invalidate its evidence bindings, but treating its embedded pre-run eligibility strings as current could reopen exhausted identities. The typed overlay binds the terminal disposition and every superseded profile/contract hash, is cross-bound from project state and the experiment registry, and makes both registered plan IDs blocked and nonreplayable in runtime policy. | No registered EXP-0001 profile is executable. A future run requires a newly registered successor profile, fresh identities and clock, reviewed clean package, source-bound runtime decision, and fresh current-turn authorization; changing flags on a consumed profile cannot restore authority. |
| D-044 | 2026-08-14 | Stop `PLAN-EXP0001-PILOT-V6` at `t09-pilot-blocked-material-risk` after one consumed infrastructure-invalid unscored Task A reactive attempt; exhaust both Retry 4 launches and all V6 execution identities, preserve the maximal private prefix, and assign no pair, comparison, or EXP-0001 outcome. | Retry 4 repaired archive staging and campaign clocks. Its final host imported and fully qualified retained image `sha256:abe8ed38…`, froze manifest `c633c835…`, and entered Task A reactive exactly once. After 20 task calls, 38,779 tokens, five requested browser actions, and four post-action results, a 234,479,616-byte core artifact exceeded the 67,108,864-byte raw-output cap. The host stopped the container with exit 143 and the immutable raw seal failed closed, so no evaluator or score exists. Both hosts are terminal/absent, zero T09 instances and restored security are verified; Retry 4 cost USD 1.701030475910902 and cumulative T09 cost USD 5.742450611151172. | Preserve V6 as operational failure evidence. Before any fresh calibration request, disable or strictly bound core-dump creation and prove that a cap stop produces a reconstructable privacy-safe raw failure seal; mint fresh identities and obtain independent review plus new current-turn authorization. Do not expand to a larger pilot from this record. |
| D-045 | 2026-08-14 | Preserve Retry 4's original four-file sealed archive and provider receipts byte-exact, and close two post-termination evidence-interface gaps with one additive private sidecar plus one source-bound clock reconciliation. | Independent review found that aggregate staging excluded a manifest-owned 597,140-byte nested archive and that the raw slot-2 closeout labeled provider-owned active time without retaining the empirical origin. Reviewed offline repair `13cbf56e…` adds only member `18f6c7d6…`; sidecar manifest `1eedf1d9…` reconstructs frozen manifest `c633c835…`, while receipt `989d5625…` derives 2,353.116390 preflight seconds and 680.255410 empirical-to-terminal seconds from preserved entry/frozen/closeout sources. It makes no pretermination-transfer claim and changes no scientific record. | Retain the original archive together with its private access-controlled sidecar; keep public release blocked. A future successor must fix the aggregate producer and core-dump/raw-seal primitive under fresh identities, review, and current-turn authorization rather than replaying V6. |
| D-046 | 2026-08-14 | Preserve Retry 4 and register fresh calibration successor `PLAN-EXP0001-PILOT-V7`; make process-tree `RLIMIT_CORE=(0,0)` and an independently capped privacy-safe essential-failure seal mandatory before its current-turn execution. | Retry 4's 234,479,616-byte Chromium-family core, rather than its 2,076,704 bytes of non-core evidence, caused the 67,108,864-byte cap failure. V7 keeps the cap and every scientific field unchanged, adds Docker and entrypoint core suppression, exact-image abort/inheritance/browser proofs, bounded filename-plus-ELF detection, remote destruction, and a 16,777,216-byte allowlisted failure authority for a consumed infrastructure-invalid attempt. Fresh V7 identities exclude the V6 attempt and preserve zero retry. | The source-bound V7 qualification or current-turn execution reaches a terminal disposition. Any later campaign requires new identities, control update, review, and authority; V6 remains consumed and nonreplayable. |
