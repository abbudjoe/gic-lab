# EXP-0000 Template

This directory is a template, not a registered or executed experiment. Copy it to the next assigned `EXP-NNNN` directory, replace every template value, register the protocol in `experiments/registry.yaml`, and validate before execution.

`results-summary.json` is intentionally and explicitly `not-run`. Do not add placeholder measurements: unknown and unexecuted data must remain absent.
